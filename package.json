{
  "name": "whatsapp-bot-platform",
  "private": true,
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "start": "cd backend && npm start",
    "build": "cd frontend && npm run build"
  }
}
